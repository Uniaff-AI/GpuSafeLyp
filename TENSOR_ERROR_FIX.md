# 🚨 Исправление ошибки "Sizes of tensors must match" в LatentSync

## ❌ Ошибка:
```
RuntimeError: Sizes of tensors must match except in dimension 1. 
Expected size 4 but got size 2 for tensor number 1 in the list.
```

## ✅ Решения:

### 1. Используйте исправленный воркфлоу:
**Файл:** `input/latentsync_stable_workflow.json`

### 2. Критические настройки:

#### 🔧 VideoLengthAdjuster (ОБЯЗАТЕЛЬНО):
- **mode**: `match_audio` (НЕ pingpong!)
- **fps**: `25` (точно!)
- **silent_padding_sec**: `0` (НЕ 0.5!)

#### 🔧 LatentSyncNode:
- **inference_steps**: `4` (консервативно, вместо 6-8)
- **lips_expression**: `1.0` (умеренно, вместо 1.5+)
- **seed**: фиксированный (например, 42)

#### 🔧 VHS_LoadVideo:
- **force_rate**: `25` (точно!)  
- **custom_height**: `512` (максимум для LatentSync 1.6)
- **format**: `AnimateDiff`

### 3. Рекомендации по входным данным:

#### 📹 Видео:
- **Длительность**: 5-30 секунд (оптимально)
- **FPS**: 25 или 30
- **Разрешение**: до 512x512
- **Формат**: MP4, AVI
- **Кодек**: H.264

#### 🎵 Аудио:
- **Формат**: WAV (лучше) или MP3
- **Sample Rate**: 44.1kHz или 48kHz
- **Битрейт**: 320kbps для MP3
- **Длительность**: соответствует видео ±1 секунда

### 4. Пошаговое исправление:

1. **Загрузите исправленный воркфлоу:**
   - Откройте http://89.208.11.177:8190
   - Load → `latentsync_stable_workflow.json`

2. **Проверьте настройки VideoLengthAdjuster:**
   - mode: `match_audio`
   - fps: `25`
   - silent_padding_sec: `0`

3. **Уменьшите нагрузку на LatentSync:**
   - inference_steps: `4` (вместо 6-8)
   - lips_expression: `1.0` (вместо 1.5+)

4. **Загрузите качественные входные данные:**
   - Видео: до 30 сек, 25fps, до 512px высота
   - Аудио: WAV 44.1kHz или MP3 320kbps

### 5. Если ошибка все еще возникает:

#### Проверьте размеры данных:
```bash
# В ComfyUI логе найдите информацию о размерах:
tail -n 100 ~/ComfyUI/ComfyUI_main/comfyui_gpu0.log | grep -i "shape\|size\|dimension"
```

#### Попробуйте другие настройки:
- Уменьшите разрешение видео до 256x256
- Сократите длительность до 10-15 секунд  
- Используйте более простой аудиофайл (моно вместо стерео)

### 6. Альтернативный воркфлоу (минимальный):

Если основной воркфлоу не работает, попробуйте:
- inference_steps: `2` (минимум)
- lips_expression: `0.8` (слабо)
- Видео: до 10 секунд, 256px высота

## 🎯 Итог:

Основная причина ошибки — несоответствие размеров тензоров аудио и видео из-за:
1. Неправильных настроек VideoLengthAdjuster
2. Слишком высокой нагрузки (много шагов инференса)
3. Неподходящих входных данных

**Используйте исправленный воркфлоу и рекомендованные настройки!**
